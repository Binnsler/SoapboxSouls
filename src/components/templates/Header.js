// Libraries
import React from "react";

// Lodash
import {
    filter,
    map,
    sampleSize,
    shuffle
} from "lodash";

function Header(){
    return(
        <div className="header">
            <h1>Header</h1>
        </div>
    );
}

export default Header;
